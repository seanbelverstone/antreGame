  const authController = require("../../controllers");

export default function(app) {
    app.route("/api/auth/")
        // .post(authController.validate);
        // Uncomment when other routes are created
}